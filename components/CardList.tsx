import { FC } from "react";
import Pagination from "./Pagination";

const CardList: FC = ({}) => {
  return (
    <div>
      CardList
      <Pagination />
    </div>
  );
};

export default CardList;
